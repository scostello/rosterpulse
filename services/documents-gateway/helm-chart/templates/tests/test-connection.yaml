apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "documents-gateway.fullname" . }}-test-connection"
  labels:
    {{- include "documents-gateway.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "documents-gateway.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
